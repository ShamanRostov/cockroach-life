# План дальнейших работ — Trader

Исследовательский пайплайн: новости (РФ + зарубежье) → зависимости с ценами голубых фишек ММВБ (≥70%) → почасовой long/short.

## Статус сейчас

- Цены MOEX ~6 месяцев по голубым фишкам — есть
- Live RSS + архив GDELT (секторные запросы) — расширяем
- Правила валидны только при hit rate ≥70%, min samples ≥10 и (опционально) Wilson lower bound ≥70%
- UI/API на `:8787`, почасовой scheduler

---

## Фаза 1 — Данные и выборки (сейчас → следующий спринт)

1. **Дожать архив новостей**
   - GDELT sectors на 182 дня (уже в прогоне)
   - Доп. проход `--mode tickers` по слабым тикерам из `ticker_coverage.json`
   - Добавить 2–3 RU-источника (ТАСС / Finam / Investing RSS), если стабильно открываются
2. **Качество выборки**
   - Дедуп заголовков — сделано
   - Порог движения цены `min_move_pct=0.3%` — сделано
   - Wilson CI + min_samples=10 — сделано
   - Разделить in-sample / out-of-sample (70/30 по времени), чтобы не переобуться
3. **Разметка**
   - Словари тональности → лёгкая ML-модель (ruBERT / multilingual-sentiment) на размеченном подмножестве
   - Явные event-типы: санкции, дивиденды, ставка ЦБ, авария, M&A

## Фаза 2 — Прогноз и риск

1. Горизонты 1h / 4h / 1d рядом, выбирать лучший по OOS hit rate
2. Размер позиции по уверенности (wilson_lower, samples), а не «всё или ничего»
3. Запрет конфликтующих long+short по одному тикеру в одном часе — частично есть
4. Бумажный journal сделок (paper trading) и equity curve

## Фаза 3 — Онлайн-продукт

1. Дашборд: покрытие по тикерам, свежие новости, почему сработал сигнал
2. Алерты (Telegram) только по правилам с wilson_lower ≥70%
3. Расписание: RSS каждые 15 мин, GDELT раз в сутки, analyze раз в час, forecast каждый час
4. Вынести Trader в отдельный GitHub-репозиторий и Cloud Agent environment

## Фаза 4 — Надёжность

1. Тесты на детерминизм разметки и на порог 70%
2. CI: bootstrap smoke (без полного GDELT)
3. Мониторинг деградации hit rate (rolling 30 дней)
4. Дисклеймер и отказ от «инвест-рекомендации» в UI/API — уже есть, не снимать

---

## Приоритет ближайших 3 шагов

1. Завершить текущий GDELT backfill → `bootstrap --skip-prices` → смотреть `valid_rules` и `ticker_coverage`
2. Докачать тикеры с `samples < 10` точечным `--mode tickers`
3. Сделать walk-forward OOS отчёт (таблица hit rate train vs test)

## Метрики успеха

| Метрика | Цель |
|---------|------|
| Новостей в архиве | ≥ 5 000 уникальных за 6 мес |
| Тикеров с ≥1 valid rule | ≥ 10 из ~20 |
| Min samples у valid rule | ≥ 10 |
| OOS hit rate valid rules | ≥ 70% (как in-sample порог) |
| Ложные срабатывания (спорт/шум) | ≈ 0 на ручной проверке 50 заголовков |

/** Phaser registry keys used by screenshot dev mode (safe to import from scenes). */
export const SS_REGISTRY = {
  ACTIVE: 'screenshot.active',
  NEST_PANEL: 'screenshot.nestPanel',
  SKIP_DAILY_POPUP: 'screenshot.skipDailyPopup',
  DUAL_PANELS: 'screenshot.dualPanels',
  SLIPPER_MID: 'screenshot.slipperMid',
  RAID_INFILTRATE: 'screenshot.raidInfiltrate',
} as const;
